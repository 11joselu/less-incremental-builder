const expect = require('chai').expect;

describe('Initial test', () => {
  it('Should return the correct imports files', () => {

  });

  it('Should return an empty array in not imports strings founded', () => {

  });

  it('Should return only imported files inside a combined strings', () => {

  });
});
